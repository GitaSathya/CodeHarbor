# Storage Configuration
# Choose one: 'memory' (default), 'file', or 'postgres'
STORAGE_TYPE=memory

# Database Configuration (only needed if STORAGE_TYPE=postgres)
# DATABASE_URL=postgresql://username:password@localhost:5432/codeharbor

# Gemini AI Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Server Configuration
PORT=5000
HOST=localhost

# File Storage Configuration (only needed if STORAGE_TYPE=file)
# DATA_DIR=./data

# Email Configuration (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
